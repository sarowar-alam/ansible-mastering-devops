# See README.md for the full workflow. This config assumes it is run from
# within the ansible/ directory (e.g. `cd ansible && ansible-playbook ...`).
[defaults]
inventory = inventories/aws_ec2.yml
roles_path = ./roles
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent
stdout_callback = default
